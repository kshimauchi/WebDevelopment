
//convert to js
interface Color {
    red: number;
    blue: number;
    green: number;

}

const color: Color = {
    red: 15,
    blue: 10,
    green: 10
};
console.log(color);

export default Color;